# Cleanup Inventory — v6.20.30 baseline (2026-05-05)

Read-only audit. ~33,049 total source lines across `dllmain.cpp` + 15 `.inl` + 6 `.h`.

## 1. Dead functions

- `tickFGKInjectionTest` at `dllmain.cpp:1840` — body is `return;` on first line (line 1850); the rest (~140 lines) is unreachable. Conclusion already documented in `datatable-fgk-cache-revisit.md`. **Safe to remove** (function + caller at `dllmain.cpp:3802` + helper `resolveHandleDataTableChanged` if not used elsewhere — verify).
- `tickActorLookupDiag` at `dllmain.cpp:1982` — one-shot diagnostic guarded by `m_actorLookupDiagDone`; only callsite is `dllmain.cpp:3801`. v6.9.0 byte-layout dump, mission accomplished. **Safe to remove** (~58 lines + member field `m_actorLookupDiagDone`).
- `tickFGKDiscoveryDiag` at `dllmain.cpp:2042` — one-shot DM.Recipes probe; only callsite `dllmain.cpp:3800`. **Safe to remove** (~33 lines + `m_dmDiagDone`).
- `cacheRecipeHandleForSlot` at `moria_placement.inl:1031` — still has 2 callsites (`dllmain.cpp:3739`, `moria_placement.inl:954`), but its only durable side-effect is setting `hasHandle = true`; the `recipeHandle[]` bytes it writes are never read post-v6.20.26. **Maybe** — function shrinks to a 2-line "set hasHandle flag" if `recipeHandle[]` is removed (see #2).
- `showRenameDialog` at `moria_widgets.inl:5938` — the function's own comment at line 5934 says "Dead code in v6.20.30 — left in place as fallback... Will be removed in the cleanup batch." Two callsites are fallbacks inside `showRenameDialog_v2` (lines 5861, 5870) plus one at `dllmain.cpp:3226`. **Needs-review** — deciding factor is whether the modal class ever fails to resolve in shipping; if v2 has been stable, drop it.
- `onInitializeNavBarPre` at `moria_settings_ui.inl:4911` — comment at `dllmain.cpp:860` says "Legacy v0.4 hook — keep in case some path still calls it." **Needs-review** — verify via VLOG whether it ever fires; if not, drop with the dispatch at line 861-865.

## 2. Dead member fields

- `RecipeSlot::recipeHandle[16]` at `dllmain.cpp:278` — written 3 places (`moria_quickbuild.inl:1042/1061`, `moria_placement.inl:1040`), **never read**. Confirmed via `grep "\.recipeHandle\b"`. v6.20.26 unified DIRECT path always rebuilds via `buildSyntheticRecipeHandle`. **Safe to remove** (also drops the constant `RECIPE_HANDLE_SIZE` usage in `cacheRecipeHandleForSlot` — but that constant is still needed for stack handles at `dllmain.cpp:3732`, `moria_placement.inl:1316/1411`).
- `m_actorLookupDiagDone`, `m_dmDiagDone`, `m_fgkInjectionTestDone` — coupled to dead functions in #1. **Safe to remove** with their owners.
- `RecipeSlot::hasHandle` — read at `dllmain.cpp:2141, 3702, 3741`, `moria_placement.inl:847, 1289`. Logic: `hasHandle == false` means "needs handle resolution by Resolving phase." Still load-bearing in handle-resolve state machine. **KEEP**.

## 3. `if (false)` and `#if 0` blocks

- `dllmain.cpp:2115` `if (false /* v0.13 disabled */ && m_characterLoaded && !m_mcBarWidget)` — MC bar auto-spawn. **Safe** to delete the whole block (and the symmetric one at 2125 for AB bar) — destruction is already handled at lines 2112-2113.
- `dllmain.cpp:2448` `if (false && vk != 0 && s_bindings[BIND_AB_OPEN].enabled)` — NUM+ AB-open dispatch. Comment at 2444-2447 explains it's superseded. **Safe to remove** (~30 lines).
- `moria_settings_ui.inl:3527-3575` `#if 0  // legacy v0.15 highlight code path — replaced by SetActiveWidget` — 48 lines. **Safe to remove**.

## 4. Stale TODO/FIXME/HACK

- `moria_join_assets.h:51` `TODO: locate /Game/UI/textures/...` — joinworld UI shipped (v6.6.0) and is stable per memory. **Stale**.
- `moria_session_history.inl:59` `// plaintext for now (TODO: obfuscate)` — current behaviour by design; flag if password obfuscation is ever requested.

## 5. Duplicate / redundant comments

Block comments in `dllmain.cpp` lines 2100-2114 (15-line v6.20.30 explanation) and 2444-2447 (NUM+ rationale) — useful for archaeology but verbose; could compress to one line each post-removal.

## 6. Version markers stuck on legacy versions

216 occurrences of `vN.M —`/`vN.M -` across 10 files. `moria_settings_ui.inl` alone has 104. The `v0.x` markers (47 in dllmain.cpp) date the Settings-UI-takeover sub-version scheme; mostly archaeological noise once the surrounding feature is stable. **Recommendation**: keep markers only on hooks/observable behaviour; drop on internal helpers. Concrete examples: `dllmain.cpp:373` "v0.8", `:1040` "v0.35", `:1338` "v0.50".

## 7. Orphaned files / inline-includes

None. All 15 `.inl` are included from `dllmain.cpp` (lines 239-601) and all `.h` are referenced. `moria_overlay.cpp` is in `CMakeLists.txt`.

## 8. Dead VLOG calls

- `dllmain.cpp:876` `[NavBarDiag] PE-pre on NavBar class: '{}'` — fires inside global PE-pre hook every PE on `UI_WBP_NavBar_Build_C`; bounded to 50 unique names per session via `s_seenNavbarFns`, but the per-call wcscmp + set lookup runs on EVERY navbar PE even after capping. v6.9.0 discovery diag. **Safe to remove** (~12 lines including the `s_seenNavbarFns` static).
- `[FGKDiag]` cluster at `dllmain.cpp:1684, 1693, 1714, 1733, 1743, 1748` — only invoked from one-shot diagnostic (similar pattern). **Needs-review** — confirm the host function is itself one-shot before dropping.

## Summary

- Total dead lines estimated: **~350** (140 tickFGKInjectionTest + 60 tickActorLookupDiag + 35 tickFGKDiscoveryDiag + 48 #if 0 + 30 if(false) blocks + 16-byte field × 9 slots + misc).
- Highest confidence removals (no risk): **5 items** — three `tick*Diag/InjectionTest` functions + their callers, two `if (false)` builder-bar gates, `#if 0` legacy highlight, NavBarDiag VLOG, `RecipeSlot::recipeHandle[]` byte buffer.
- Needs-review items: **3** — `showRenameDialog` legacy fallback, `onInitializeNavBarPre` legacy hook, `[FGKDiag]` VLOG cluster (verify caller is one-shot).
